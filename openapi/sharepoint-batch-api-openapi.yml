openapi: 3.2.0
info:
  title: SharePoint REST $batch $batch $batch $batch API
  description: Minimal OpenAPI definition for the SharePoint Online REST/OData web service exposing site, web, list, and list item resources.
  version: '1.0'
  x-generated-from: https://learn.microsoft.com/en-us/sharepoint/dev/sp-add-ins/get-to-know-the-sharepoint-rest-service
  x-generated-by: claude-crawl-2026-05-08
servers:
- url: https://{tenant}.sharepoint.com/_api
  description: SharePoint site collection root REST endpoint
  variables:
    tenant:
      default: contoso
security:
- bearerAuth: []
tags:
- name: $batch
paths:
  /$batch:
    post:
      summary: Submit OData $batch request
      operationId: batch
      requestBody:
        required: true
        content:
          multipart/mixed:
            schema:
              type: string
      responses:
        '200':
          description: OK
      tags:
      - $batch
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
